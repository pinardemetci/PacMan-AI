                    # self.features=[NearestCapsuleFeature(),NearestScaredGhostFeature()]
                    # normal_ghosts = filter(lambda n: n.scaredTimer == 0, ghosts)
                    # scared_ghosts = filter(lambda g: g.scaredTimer > 0, ghosts)
                    # if len(normal_ghosts)>0:
                    #     self.features.append(NearestNormalGhostFeature())
                    # if len(normal_ghosts)>0 and NearestNormalGhostFeature() in self.features:
                    #     self.features.remove(NearestNormalGhostFeature())